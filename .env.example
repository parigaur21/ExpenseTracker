# .env.example
# Database URL for Prisma (SQLite for quick start, replace with PostgreSQL in production)
DATABASE_URL="file:./dev.db"
# JWT secret for signing tokens (change to a strong secret in production)
JWT_SECRET="your-very-secret-key"
# Server port (default 4000)
PORT=4000
